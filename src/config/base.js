// Settings configured here will be merged into the final config object.
export default {
  defaultColor: 'red',
  defaultLineWidth: 20,
  maxLineWidth: 500,
  penPreviewTimeout: 500,
  durationStep: 0.5
};
